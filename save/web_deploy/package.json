{
  "name": "skin-capture-web",
  "version": "1.0.0",
  "private": true,
  "description": "Browser port of capture_prototype.py \u2014 three-angle face capture with per-region skin segmentation, on-device.",
  "type": "module",
  "scripts": {
    "dev": "npx --yes serve@14 -l 8000 .",
    "tunnel": "npx --yes cloudflared tunnel --url http://localhost:8000",
    "test": "node --test \"test/*.test.js\"",
    "tunnel:lt": "npx --yes localtunnel --port 8000"
  }
}
